# Examples

`src/index.ts` contains type-checked examples for a custom Node, executable math graph, decorative architecture map, Machine API graph creation, bounded streaming and Universal Import. The standalone application adds a Container and optional Timeline example.

These sources are part of the root `tsc --noEmit` check, so SDK changes cannot silently break the documentation snippets.
