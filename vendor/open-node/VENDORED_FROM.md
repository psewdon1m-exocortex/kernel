# Vendored Open Node

This directory is a source snapshot of `../../open-node` taken on 2026-07-25.

Kernel owns this snapshot so its Topology Map does not depend on a published
package or a sibling runtime. The integration adds a `visualOnly` mode and
disables executable features in Kernel.

The original project is MIT licensed; see `LICENSE`.
